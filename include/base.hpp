/**
 * 1. 矩阵操作
matrixAdd：矩阵加法。

matrixMultiply：矩阵乘法。

matrixTranspose：矩阵转置。

matrixInverse：矩阵求逆。

matrixDeterminant：计算矩阵的行列式。

matrixScalarMultiply：矩阵与标量相乘。

matrixScalarAdd：矩阵与标量相加。

2. 矩阵分解
LUdecomposition：LU 分解。

QRdecomposition：QR 分解。

CholeskyDecomposition：Cholesky 分解。

SVD：奇异值分解。

3. 向量操作
vectorAdd：向量加法。

vectorSubtract：向量减法。

vectorDotProduct：向量点积。

vectorCrossProduct：向量叉积。

vectorNormalize：向量归一化。

vectorScalarMultiply：向量与标量相乘。

vectorScalarAdd：向量与标量相加。

4. 矩阵与向量运算
matrixVectorMultiply：矩阵与向量相乘。

vectorMatrixMultiply：向量与矩阵相乘。

5. 特征值与特征向量
eigenvalueDecomposition：特征值分解。

eigenvectors：计算特征向量。

eigendecomposition：特征值和特征向量同时计算。

6. 优化与高级功能
leastSquares：最小二乘法求解。

pseudoInverse：伪逆计算。

QRLeastSquares：QR 分解最小二乘法。

JacobiSVD：Jacobi 奇异值分解。

7. 矩阵求和与积
matrixSum：矩阵元素求和。

matrixProduct：矩阵元素逐个乘积。

matrixTrace：计算矩阵的迹（对角线元素之和）。

8. 矩阵对角线和其他功能
matrixDiagonal：提取矩阵对角线。

matrixRow：提取矩阵某一行。

matrixColumn：提取矩阵某一列。
 */
